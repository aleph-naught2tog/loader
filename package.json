{
  "name": "loader",
  "version": "1.0.0",
  "description": "goal: * be able to use node_modules/naked imports normally in code * have those automagically be where i need them when we load up",
  "devDependencies": {
    "@types/node": "^10.12.21",
    "jest": "^24.1.0"
  },
  "scripts": {
    "test": "jest --verbose --watch",
    "start:project": "rm -rf ./demo_project/build ./demo_project/node_modules && node build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
